spring:
  datasource:
    url: jdbc:mysql://localhost:8200/security
    username: root
    password: root
  jpa:
    open-in-view: false
    generate-ddl: true
    show-sql: true
    properties:
      hibernate:
        format_sql: true
logging:
  level:
    org.springframework.security.web.FilterChainProxy: DEBUG