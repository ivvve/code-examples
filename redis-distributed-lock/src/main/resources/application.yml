spring:
  redis:
    host: localhost
    port: 6379
    database: 0
  datasource:
    url: jdbc:mysql://localhost:3306/playground
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: create
